export async function searchMedia(results) {
  const dofsee = {
    // funcvar:
    createTrue: true,
  };

  let endFind = "";
  switch (results.mediaType) {
    case "book":
      //   endFind = await filtModel(
      //     "media",
      //     "googleBooksID",
      //     oksqweas.googleBooksID,
      //     dofsee
      //   );
      break;

    default:
      break;
  }

  return endFind;
}
